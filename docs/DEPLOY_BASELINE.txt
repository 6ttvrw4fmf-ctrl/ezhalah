e577005ea2545681834283f210631babb55c1698
# APPROVED PRODUCTION BASELINE — the commit currently live & approved on ezhalah-app.vercel.app.
# The first line MUST be the full/short commit SHA. scripts/preflight-verify.sh refuses any deploy
# whose HEAD does NOT contain this commit (that would drop approved UI — the 2026-07-09 incident).
# safe-deploy.sh advances this to the newly-deployed SHA after every successful production deploy.
# Do NOT hand-edit the SHA except to record a deliberate, verified rollback target.
#
# 2026-07-15/16  61194df  DELIBERATE ROLLBACK (owner-approved, "DEPLOY NOW"): the prior baseline
#   (016bee3) was recorded via safe-deploy.sh AFTER PR #78 (42cd105) shipped — but PR #78's frontend
#   (src/components/AdvancedQuestionCard.tsx, src/data/advancedFilters.ts, and the age-filter wiring
#   in agent.tsx/remote.ts/search.ts/i18n.tsx) caused a real production outage: search hung
#   indefinitely for real users. PR #82 reverted that frontend (merged); this baseline update
#   records that removal as INTENTIONAL, not an accidental UI drop, so preflight-verify.sh's
#   file-deletion guard stops correctly-but-unhelpfully flagging the fix itself as data loss. See
#   project_pr78-outage-rollback-2026-07-15 (session memory) for the full incident record. Also
#   includes PR #83 (safe-deploy.sh's new blocking live-search smoke test, added specifically so
#   this class of outage is caught automatically before the baseline ever advances again).
#
# 2026-07-10  ef6a3ae  Filter UX: partial-reveal scroll, Monthly/Yearly reposition, full-box tap targets (#48)
# 2026-07-10  a6a8a37  deployed via safe-deploy.sh
# 2026-07-10  1936fc0  deployed via safe-deploy.sh
# 2026-07-10  75b8970  deployed via safe-deploy.sh
# 2026-07-11  119e753  deployed via safe-deploy.sh
# 2026-07-11  a2a5989  deployed via safe-deploy.sh
# 2026-07-12  bc4b455  deployed via safe-deploy.sh
# 2026-07-13  9740898  deployed via safe-deploy.sh
# 2026-07-13  233a095  deployed via safe-deploy.sh
# 2026-07-14  c0304db  deployed via safe-deploy.sh
# 2026-07-15  da19962  deployed via safe-deploy.sh
# 2026-07-15  42cd105  deployed via safe-deploy.sh
# 2026-07-15  016bee3  deployed via safe-deploy.sh
# 2026-07-16  e7eb6ba  deployed via safe-deploy.sh
# 2026-07-16  d6ed2e0  deployed via safe-deploy steps (manual, deploy-lock held via MCP)
# 2026-07-16  1ab61d7  deployed via manually-reproduced safe-deploy.sh sequence (SUPABASE_SERVICE_ROLE_KEY not set; lock via MCP execute_sql per AGENTS.md)
# 2026-07-16  40ca85b  deployed via safe-deploy.sh-equivalent (PR #87 tsc-blockers, on top of PR #83-86, #69, #72)
# 2026-07-16  d663c7d  deployed via safe-deploy.sh
# 2026-07-16  fe3f3c5  deployed via safe-deploy.sh
# 2026-07-16  287e113  deployed via safe-deploy.sh-equivalent (PR #104 English-address-leak fix on
#   result cards, deploy-lock via MCP — SUPABASE_SERVICE_ROLE_KEY not set; waited ~9min for a
#   concurrent session's active DB migration to release the lock before deploying, per AGENTS.md)
# 2026-07-16  e9c8853  deployed via safe-deploy.sh
# 2026-07-16  8bdd26a  deployed via safe-deploy.sh-equivalent (PR #112 Arabic-only residential
#   sweep: card-title guard, 4 new satel enum translations, dealapp facade free-text guard,
#   2 search.ts + 1 agent.tsx interpolation fixes; deploy-lock via MCP)
# 2026-07-17  e577005  deployed via safe-deploy.sh-equivalent (dpl_4EbNG1YrpYDSBEfA8xZqmbG7XnGs).
#   PR #138 City-only Location field ("أي مدينة؟"): replaces the combined city-or-neighborhood
#   field with a strict city_id-keyed picker — Top-6-by-listings on focus (new read-only view
#   city_listing_counts_ar, already applied to prod separately), Arabic-only autocomplete over
#   ~423 cities with real listings, no free text accepted. Two real bugs found+fixed during
#   verification: an async/sync race that could clobber typed results with stale Top-6 data, and
#   a slow-connection gap where typing before the city data finished loading left the dropdown
#   empty forever. Verified: 344/344 tests, live production data re-check against the owner's own
#   autocomplete examples, and a live anon-key confirmation that location_search_candidates_ar's
#   existing city_id + region_id double-match (city_tokens/city_ids CTEs + p_region_ids filter)
#   correctly disambiguates same-named-but-different cities end-to-end. Baseline advance done
#   manually — the automated safe-deploy.sh drift gate found 17 migrations from other concurrent
#   sessions not yet in this repo checkout (0 duplicate overloads, not the PGRST203 outage class).
# 2026-07-17  7576669  deployed via safe-deploy.sh + manual vercel rollback promotion (dpl_DMhwSD3P6wGs9174iWKnr7J5kiBi).
#   PR #130 null-category-leak fix (residential-commercial-isolation-audit-2026-07-17): deselecting
#   the category pill sent p_category=null, disabling the RPC purity gate + client matchesType()
#   safety net simultaneously, letting ~14.4k misfiled Commercial-macro rows leak into a "nothing
#   selected" search (1,202 rows on a live Riyadh+Buy test). Fixed via impliedCategory() defaulting
#   to Residential when nothing is selected, matching kindsFor()'s own pre-existing default. Bundles
#   4 unrelated PRs already merged but never previously deployed: #125 (aqar comma-digit parsing),
#   #126 (rent-period payment_monthly re-apply), #127 (aqar liveness/price_per_meter), #129
#   (furnishing i18n dual-label fix). safe-deploy.sh's schema-drift gate blocked the AUTOMATED
#   baseline advance (5 migrations applied to prod by other concurrent sessions, not yet in this
#   repo checkout — 0 duplicate overloads, so NOT the PGRST203 outage class, just a bookkeeping gap;
#   flagged separately, not fixed here) — this line records the advance manually after independent
#   verification: (a) live anon-key RPC delta reproduced (37,686→36,483 Riyadh+Buy, matches audit),
#   (b) 3-lens adversarial code review (correctness/product-contract/completeness) all SAFE, (c) a
#   suspected "click-dead" page-wide hydration issue found via the Claude_Browser pane turned out to
#   be a TESTING-TOOL ARTIFACT, not a real bug — confirmed via a real Chrome session (claude-in-chrome
#   MCP): category pill, dropdown, and full search-to-results flow all work correctly end-to-end,
#   confirmed for both this deploy and prior ones. (A same-session emergency rollback to 05ff2528
#   was performed then reverted once this was established — see project memory for the full story.)
