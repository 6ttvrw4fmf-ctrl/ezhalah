# Retired platform slugs — one per line.
#
# CONTRACT: a slug listed here must NEVER appear as an active (non-commented) `source:` entry in
# any .github/workflows/*.yml platform matrix. Enforced by
# scrapers/common/tests/test_retired_platforms_guard.py (hermetic, zero extra deps; runs in CI via
# common-location-tests.yml). This guard exists because retirement did NOT stick the first time:
# toor was removed 2026-07-06 (commit d5ca9d6, owner-approved) and PR #77 (da19962) silently
# re-added its matrix line eight days later, putting a dead platform back on the daily cron.
#
# To RETIRE a platform: add its slug here + remove its matrix line (leave a NOTE comment in the
# workflow explaining why, like the toor/alnokhba ones in small-sources-sync.yml). Historical DB
# rows and the scrapers/<slug>/ directory are KEPT.
# To UN-RETIRE one (owner approval required): remove the slug here in the SAME PR that re-adds
# the matrix line, referencing the approval in the commit message.
#
# toor      — retired 2026-07-06 (owner-approved): toor.ooo IP-blocks datacenter + Saudi-proxy
#             IPs; every fetch timed out, 0 rows for weeks.
# alnokhba  — deprecated 2026-07-14: alnokhba-services.com lapsed into a domain-parking page
#             (HTTP 200 shell, no listings); 7 straight daily runs of rows_seen=0.
# muktamel  — paused 2026-07-15: runs on its OWN gated weekly workflow (muktamel-sync.yml), never
#             the shared matrix; listed here so it cannot re-enter any matrix while paused.
# deal      — deprecated 2026-06-26 (live deprecated_platforms row; experiment reverted
#             2026-06-24): a 2-run JSON-API experiment against api.dealapp.sa — the SAME site the
#             active `dealapp` pipeline covers, so running both would double-list dealapp.sa
#             inventory. 36 DB rows retained, never user-visible (0 active); freshness-excluded
#             via the hardcoded `'deal\_%'` literal in check_scraper_freshness(). Listed here
#             2026-07-16 (Batch 3) so the slug can never enter a workflow matrix. If the JSON API
#             is ever wanted, evolve scrapers/dealapp/ instead of un-retiring this slug.
# jazwtn   — UN-retired 2026-07-27 same day (owner-instructed) once PR#230 gave it the
#             residential-proxy egress the retirement note required; entry kept for history.
#             WAS retired 2026-07-27 (owner-approved): jazwtn.sa IP-blocks GitHub Actions runner
#             ranges. 6/6 consecutive failed runs from 2026-07-23, always on the first request
#             (sitemap) with curl(35) "Connection reset by peer"; the SAME URL returns HTTP 200 /
#             68,410 bytes in 0.26s from a non-runner IP, so the site and the scraper code are
#             both fine — only the egress IP is blocked (same class as toor). 137 DB rows are
#             KEPT ACTIVE and user-visible; the sitemap held 138 URLs with newest lastmod
#             2026-07-13, so nothing new was being missed at retirement. Un-retiring requires a
#             non-runner egress IP (residential proxy), not just re-adding the matrix line.
toor
alnokhba
muktamel
deal
awal  # domain parked + link-rot inactivated 2026-07-28 (PR#252/#255); 0 active rows
