# Retired platform slugs — one per line.
#
# CONTRACT: a slug listed here must NEVER appear as an active (non-commented) `source:` entry in
# any .github/workflows/*.yml platform matrix. Enforced by
# scrapers/common/tests/test_retired_platforms_guard.py (hermetic, zero extra deps; runs in CI via
# common-location-tests.yml). This guard exists because retirement did NOT stick the first time:
# toor was removed 2026-07-06 (commit d5ca9d6, owner-approved) and PR #77 (da19962) silently
# re-added its matrix line eight days later, putting a dead platform back on the daily cron.
#
# To RETIRE a platform: add its slug here + remove its matrix line (leave a NOTE comment in the
# workflow explaining why, like the toor/alnokhba ones in small-sources-sync.yml). Historical DB
# rows and the scrapers/<slug>/ directory are KEPT.
# To UN-RETIRE one (owner approval required): remove the slug here in the SAME PR that re-adds
# the matrix line, referencing the approval in the commit message.
#
# toor      — retired 2026-07-06 (owner-approved). THE ORIGINAL REASON IS NO LONGER THE REASON;
#             re-probed 2026-09-19 at the owner's instruction ("a retirement without a proper
#             excuse is a failure — if the site is up, turn it back on").
#
#             The original note said toor.ooo IP-blocks datacenter + Saudi-proxy IPs and every
#             fetch timed out. THAT IS FIXED: from a clean egress the sitemap and every detail
#             page now return HTTP 200 (sitemap_properties_1.xml → 82 URLs, detail ≈1.2 MB), the
#             same class of recovery that un-retired jazwtn (PR#230) and awal (2026-09-04). So the
#             transport excuse is dead and must not be cited again.
#
#             IT STAYS RETIRED FOR A DIFFERENT, VERIFIED REASON: toor.ooo rebuilt as an SPA and
#             its public HTML is now a SHELL that serves ONE SAMPLE LISTING for every PropertyId.
#             Measured 2026-09-19 over six different PropertyIds from the sitemap — all six
#             returned licence 7201112446 / ref 25316 / area 4944.61 / price 40,000: ONE distinct
#             listing out of six. The JSON-LD `name` is the literal placeholder «الاسم» ("the
#             name"), and the CSS classes the old parser anchored on are now randomised
#             (st__K7lM8n, st__O2pQ3r vs the shipped st__J3hQ4o).
#
#             Running scrapers/toor/ as-is would therefore write 82 COPIES OF ONE FABRICATED
#             LISTING — the invented-data failure PRICE = SOURCE exists to prevent. The real data
#             moved behind an authenticated API: /api/getProperty*, /api/getPropertyDetails*,
#             /api/property/<id> all return HTTP 401; only /api/getMediaByProperty/<id> (200, []) 
#             and /api/getCityInformation/ (200) are public.
#
#             TO UN-RETIRE, the blocker is no longer egress — it is auth + a parser rewrite. Do
#             NOT re-add the matrix line on the strength of "the site loads now": it loads, and it
#             lies. Verify first that six different PropertyIds yield six DIFFERENT licences.
# alnokhba  — deprecated 2026-07-14: alnokhba-services.com lapsed into a domain-parking page
#             (HTTP 200 shell, no listings); 7 straight daily runs of rows_seen=0.
# muktamel  — NOT RETIRED; delisted 2026-09-19. This is a CORRECTION of stale bookkeeping, not an
#             un-retirement: muktamel had already stopped being paused, and only this file still
#             said otherwise. Entry kept, like jazwtn's and awal's, so the history is readable.
#
#             WAS listed 2026-07-15 as "paused: runs on its OWN gated weekly workflow
#             (muktamel-sync.yml), never the shared matrix". The pause ended 2026-09-04/05
#             (migration 20260904151723_muktamel_liveness_policy_paused_is_not_unsearchable —
#             "paused was a CADENCE fact", and the registry re-seed that followed). Since then:
#             platform_registry carries muktamel at status='active'; search_listings_ar carries
#             4,105 searchable rows (newest last_updated 2026-09-14); it is advertised in the
#             loading strip (PLATFORM_META, src/data/loaderPlatforms.ts) with a bundled logo; it
#             holds a registered liveness policy (CRAWL_PRESENCE_ONLY/3/168); and it runs its own
#             8-shard fleet, muktamel-sharded.yml, on cron. None of that is a paused platform.
#
#             WHY IT WENT UNNOTICED FOR TWO WEEKS. Every reference to this file is a guard that
#             keeps a slug OUT of something, so a live platform listed here can only ever be
#             excluded from work it belongs in — it fails silently, in the safe-looking direction.
#             The existing matrix guard could not see it either: it matches `source:` matrix keys,
#             and muktamel's fleet is keyed on `shard:`. It also made
#             scripts/reprobe-retired-platforms.mjs skip muktamel — for the wrong reason.
#
#             test_retired_platforms_guard.py::test_no_retired_platform_is_live_in_the_liveness_registry
#             now makes this drift un-mergeable: it fails the moment a slug listed here is also
#             registered as production-searchable.
# deal      — deprecated 2026-06-26 (live deprecated_platforms row; experiment reverted
#             2026-06-24): a 2-run JSON-API experiment against api.dealapp.sa — the SAME site the
#             active `dealapp` pipeline covers, so running both would double-list dealapp.sa
#             inventory. 36 DB rows retained, never user-visible (0 active); freshness-excluded
#             via the hardcoded `'deal\_%'` literal in check_scraper_freshness(). Listed here
#             2026-07-16 (Batch 3) so the slug can never enter a workflow matrix. If the JSON API
#             is ever wanted, evolve scrapers/dealapp/ instead of un-retiring this slug.
# jazwtn   — UN-retired 2026-07-27 same day (owner-instructed) once PR#230 gave it the
#             residential-proxy egress the retirement note required; entry kept for history.
#             WAS retired 2026-07-27 (owner-approved): jazwtn.sa IP-blocks GitHub Actions runner
#             ranges. 6/6 consecutive failed runs from 2026-07-23, always on the first request
#             (sitemap) with curl(35) "Connection reset by peer"; the SAME URL returns HTTP 200 /
#             68,410 bytes in 0.26s from a non-runner IP, so the site and the scraper code are
#             both fine — only the egress IP is blocked (same class as toor). 137 DB rows are
#             KEPT ACTIVE and user-visible; the sitemap held 138 URLs with newest lastmod
#             2026-07-13, so nothing new was being missed at retirement. Un-retiring requires a
#             non-runner egress IP (residential proxy), not just re-adding the matrix line.
# awal      — UN-RETIRED 2026-09-04 (owner-instructed). WAS retired 2026-07-28: awaalun.com had
#             lapsed to a GoDaddy parking page serving 133-byte redirect stubs. RE-PROBED
#             2026-09-04 from a clean egress: the site is BACK and serving real inventory —
#             /wp-json/wp/v2/rtcl_listing returns HTTP 200 with x-wp-total: 128, and the
#             shipped scraper parses all 128 into rows with 128 DISTINCT ad_numbers, 128
#             distinct listing_urls and 128 distinct descriptions (no duplication). Newest
#             listing is 2026-02-14, so the inventory is real but STALE — recorded here so
#             nobody reads its low churn as a broken crawl. The source publishes NO prices
#             (0/128 in both the REST payload and the detail pages, verified live); that is a
#             SOURCE fact, not a parse gap, and is consistent with the 7,534 priceless rows
#             (3.83%) already live from other platforms. Nothing is inferred to fill it.
toor
alnokhba
deal
