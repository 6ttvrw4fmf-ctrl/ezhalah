playwright==1.49.1
curl_cffi==0.15.0  # 0.7.4 (mid-2024) carried a 2-year-stale chrome124 TLS fingerprint that
                   # jazwtn.sa's WAF began rejecting ~2026-07-23 (SSL reset from ANY egress IP,
                   # proxy included) while 0.15.0's chrome124 passes (verified 2026-07-27:
                   # 0.7.4=curl(35) reset, 0.15.0=HTTP 200/68,410B, same host+IP). Same
                   # "impersonate=chrome124" API on both; fleet-wide fingerprints simply become
                   # a CURRENT Chrome, which sites accept better, not worse.
supabase==2.10.0
python-dotenv==1.0.1
