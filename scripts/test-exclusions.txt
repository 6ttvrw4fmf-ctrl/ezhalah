# Checks that deliberately do NOT run in `npm test`, each with WHERE it runs instead.
#
# Format:  filename | where it DOES run | why it is not in npm test
#
# This file is the ONLY way a verify-* check can be absent from `npm test`. Everything else in
# scripts/ that matches verify-*.{ts,mjs} runs automatically, so a barrier cannot go dark by being
# forgotten — only by a deliberate, reviewed line here. An entry naming nowhere is a check that runs
# nowhere, and verify-test-registry-complete.ts fails on it.
#
# The common reason is the same one AGENTS.md gives for the migration drift guard: `npm test` is a
# REQUIRED status check on every PR, so wiring a LIVE production check into it would fail unrelated
# PRs whenever production is momentarily unhealthy — turning a required gate into noise.

verify-af-agent-cta-live.ts            | .github/workflows/af-live-truth-check.yml            | drives a real browser against production
verify-af-card-evidence-live.ts        | .github/workflows/af-live-truth-check.yml            | drives a real browser against production and proves R12A.6: every committed AF answer is visible on the returned card, carries the LISTING's own value (not the filter's label), renders nothing for a column the source never published, and never claims what the row does not satisfy — plus the negative (a search with no AF answer shows no strip and carries no af_canon). Its offline twin verify-af-card-evidence.ts stays in npm test
verify-af-independent-oracle.ts        | .github/workflows/af-oracle-pr-check.yml             | 9 hand-written PostgREST predicates + the UNKNOWN partition against production. A live call, so it stays OUT of the required npm test (the pattern #1486 removed elsewhere) — but it is a PR check again since 2026-09-04, in its own 7.8-second workflow (pull_request + push main + dispatch, no secrets so forks work, retried 3x so a production hiccup cannot fail an unrelated PR). Also runs post-deploy as a step in af-live-truth-check.yml
verify-af-matrix-truth-live.ts         | .github/workflows/af-live-truth-check.yml            | the WHOLE certified AF matrix (every scope × mode × field × option) against production RPCs and independent PostgREST truth on canonical columns; its offline twin verify-af-matrix-truth.ts stays in npm test
verify-af-live-truth.ts                | .github/workflows/af-live-truth-check.yml            | drives a real browser against production
verify-trending-live-four-way-truth.ts | .github/workflows/af-live-truth-check.yml            | drives a real browser against production
verify-af-property-type-differential.ts| .github/workflows/af-live-truth-check.yml            | sweeps every certified cohort against production (RPC vs independent DB truth)
verify-af-stale-predicate-live.ts      | .github/workflows/af-live-truth-check.yml            | drives a real browser against production
verify-af-pill-removal-live.ts         | .github/workflows/af-live-truth-check.yml            | drives a real browser against production (R9.2: removing an AF pill drops exactly that predicate, widens honestly, rewrites nothing above)
verify-af-full-surface-differential.ts | .github/workflows/af-live-truth-check.yml            | sweeps EVERY option of EVERY certified AF cohort against production: chip = af_eligible_count = search total = paged ID set = independent PostgREST truth, rows re-evaluated in JS; OR/AND combos, boundaries, NULL, zero-result, multi-type, combined deal, both periods
verify-af-option-card-truth-live.ts    | .github/workflows/af-live-truth-check.yml            | drives a real browser against production: every rendered AF option count = captured count RPC = independent oracle; click = exact set; load-more pages carry the same predicate with no duplicates
verify-af-remove-last-pill-live.ts     | .github/workflows/af-live-truth-check.yml            | drives a real browser against production: removing the LAST AF pill returns byte-exactly to the pre-AF search (zero AF predicates, same count, question not burned)
verify-af-scope-change-live.ts         | .github/workflows/af-live-truth-check.yml            | drives a real browser against production: changing deal / rent period after AF keeps exactly the answers the new cohort certifies (cohortAllows executed), drops the rest, resurrects nothing
verify-combined-budget-live.ts          | .github/workflows/af-live-truth-check.yml            | drives a real browser against production (combined شراء+إيجار: each budget binds only its own deal)
verify-trending-filter-state-live.ts   | .github/workflows/af-live-truth-check.yml            | live trending/district RPC counts vs independent PostgREST truth (the LIVE half of verify-trending-carries-full-filter-state.ts, whose SOURCE half stays in npm test)
verify-searchable-scope-matches-inventory.ts | .github/workflows/af-live-truth-check.yml       | EXECUTES the real resTables/comTables (liftSymbols) and compares the client scope to production's live searchable inventory in both directions — MISSING (a live platform no search can reach) and EXTRA (a client table production does not have). The join between a DB activation and the client scope that nothing checked before 2026-09-03
verify-trending-category-respects-table-scope.ts | .github/workflows/af-live-truth-check.yml    | discovers, fleet-wide, every row whose type says one category but whose table says the other (top_cities_by_deal_ar's known_type_ar.macro check vs the table suffix Search actually enforces via p_tables) and asserts Trending == Search == an independent table-suffix oracle, both at the fleet-wide sum and at bounded per-city depth; live production data, needs the network
verify-platform-identity-matches-live-source.ts | .github/workflows/af-live-truth-check.yml   | reads the `source` value PRODUCTION stores for every platform table and runs the real lifted sourceHost over it — the offline verify-platform-registration-complete.ts feeds the matcher PLATFORMS[i].name, a string this repo chose, so it cannot see a DB value that differs (found 2026-09-04: 'Abr Alosol'/'THE RC'/'Rawasi Dark' all fell to the Aqar fallback)
verify-chat-persistence-live.mjs       | manual / live run                                    | live check against production; no scheduled workflow yet
verify-count-rpc-parity-live.ts        | .github/workflows/count-rpc-parity-live-check.yml    | live production RPC parity
verify-district-source-truth-live.ts   | manual / live run                                    | live check against production; no scheduled workflow yet
verify-district-suggestion-parity-live.ts | .github/workflows/district-suggestion-parity-live-check.yml | live production RPC parity
verify-frontend-bundle-matches-source-live.ts | .github/workflows/frontend-bundle-source-parity-live-check.yml | fetches the live production bundle; same reason as the other live checks
verify-live-hydration.mjs              | .github/workflows/deploy-frontend.yml                | post-deploy check against the served bundle
verify-loc-classify-postgrest-latency.ts | npm run verify:loc-classify-latency                | live PostgREST-path check (2026-08-31 loc_classify timeout incident); no scheduled workflow yet
verify-location-index-source.ts        | npm run verify:location-index                        | needs the location index source; run by the taxonomy/index suite
verify-migration-content-parity.ts     | .github/workflows/migration-drift-guard.yml          | drift condition #5, live against production; same reason as the drift checker below - content divergence anywhere would fail every unrelated PR
verify-migration-drift-vs-production.ts| .github/workflows/migration-drift-guard.yml          | AGENTS.md pins it OUT of npm test: drift anywhere in production would fail every unrelated PR
verify-photo-preference-and-rotation-live.ts | .github/workflows/photo-rotation-live-check.yml | live production RPC check (photo preference + controlled rotation)
verify-platform-diversity-live.ts      | .github/workflows/diversity-live-check.yml           | live production diversity check
verify-recency-fallback-live.ts        | .github/workflows/recency-fallback-live-check.yml    | live production check
verify-region-scoped-city-live.ts      | .github/workflows/region-scoped-city-live-check.yml  | live production region+city check; also re-earns ops_qa_search_differential's city-resolution exemption
verify-residential-misfile-recovery.ts | manual / live run                                    | one-off SHADOW PROOF of the 2026-07-10 residential-misfile fix, pinned to that day's expected delta; needs live creds and no workflow has ever invoked it (the row claimed full-verification-ci.yml, which only MENTIONS it in a comment saying it is deliberately not run there)
verify-strict-filter-parity-live.ts    | .github/workflows/af-live-truth-check.yml            | live RPC total_count vs strict PostgREST ground truth for every previously NULL-permissive filter + the amenity/direction vocabulary invariants; given a real home 2026-09-03 after its old row named a workflow that never invoked it
verify-taxonomy.ts                     | npm run verify:taxonomy                              | needs network/taxonomy source; run by the taxonomy suite
verify-trending-usable-under-narrowing.ts | .github/workflows/count-rpc-parity-live-check.yml | live anon-role check against production
verify-unlocated-fallback-scope-live.ts| manual / live run                                    | live check against production; no scheduled workflow yet
verify-web-runtime-smoke.mjs           | .github/workflows/web-runtime-smoke.yml             | drives a real browser; the row named full-verification-ci.yml, which never invoked it — corrected 2026-09-03
verify-loader-platforms-match-active.ts | .github/workflows/loader-active-platforms-check.yml | queries production for the distinct active platform set to prove the loader roster is not dishonest
verify-liveness-claims-are-earned.ts    | .github/workflows/loader-active-platforms-check.yml | reads production's OWN liveness coverage (anon) and fails if a platform is represented as liveness-proven without the evidence that word requires: a CRAWL_PRESENCE_ONLY registration is a disclaimer, and a verified-alive count under it is verification nobody performed. The committed offline barrier (verify-liveness-registry-mirror.ts §4) enforces only the opposite direction, over scrapers/**/*.py, and cannot see production's verification state at all
verify-count-surfaces-share-the-results-purity-gate.ts | .github/workflows/af-live-truth-check.yml | reconciles EVERY scope-carrying count surface (discovered at run time out of src/data, never a hardcoded list) against the results RPC total_count under the identical scope. Needs the live anon path, and it is RED while ops_incident #31/#32 stand — so it belongs with the other production-truth barriers, not in npm test
verify-detector-total-matches-its-breakdown-live.ts | .github/workflows/af-live-truth-check.yml       | executes the REAL production detector functions through the publishable key and asserts a payload's scalar total equals the by_* breakdown it publishes beside it (incident #36: price_fidelity() reported mismatches=2 while its own by_platform said 1,563, because the total was count(*) over a GROUP BY — so mon_detect_price_fidelity's 250/25 row-scale thresholds were unreachable and it had never fired). A live call, and it is RED right now on a real production defect, so it must not sit in the required npm test; its 10 offline mutation proofs run either way
verify-p0-fast-lane-detection.ts        | .github/workflows/p0-fast-lane-coverage.yml          | calls production (ops_p0_detectors_off_fast_lane) to prove every P0-capable mon_detect_* is on the mon_run_p0_detectors() fast-lane roster. Live, so out of the required npm test for the same reason as the migration drift guard. Runs on pull_request, on push to main touching migrations, and every 30 min — the schedule is the load-bearing trigger, since this breaks when a migration is APPLIED, not when a PR is opened
