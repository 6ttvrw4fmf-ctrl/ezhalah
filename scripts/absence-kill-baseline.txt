# PLATFORMS THAT CAN STILL DEACTIVATE A LISTING ON ABSENCE ALONE — a SHRINK-ONLY floor.
#
# One line per `db.prune_unseen(...)` call site that is NOT passed a `verify_gone=` oracle, so the
# only thing standing between a live listing and `active = false` is three consecutive crawls that
# failed to return it. `docs/ops/LISTING_LIVENESS.md` §1-§3: absence is EvidenceKind.ABSENCE — a
# candidate signal, never a verdict — and only a DIRECT fetch of the listing's own URL may kill.
#
# Measured 2026-09-21 by routine #2 (scripts/verify-crawl-presence-only-cannot-kill.ts, which reads
# these keys back). Format: platform|strategy|ordinal of the call site within run.py (0-based).
#
# The key is an ORDINAL, deliberately not a line number: a call site must stay identifiable across
# unrelated edits above it, or an innocent diff turns this floor RED and the next person widens it.
#
# WHY A FLOOR AND NOT A BAN. Every line here is shipped code, and giving a platform a real oracle
# needs its removal signal MEASURED against the live source with known-alive controls — that is the
# platform owner's work, not a barrier's, and a guard that went red on 24 shipped scrapers would be
# deleted by the first person it blocked. So: this list FALLING is the intended direction and never a
# failure, a NEW entrant is RED, and an entry that has been fixed is RED as STALE so the ratchet can
# never read better than reality. Routed as an incident to routine-11-lifecycle with this list.
#
# muktamel is NOT here: it left this set on 2026-09-21 after alert 4344 (94 rows deactivated at
# 04:54 with no source verdict for any of them) — the incident that established this class.
CEILING=25
abralosol|CRAWL_PRESENCE_ONLY|0
abwbna|CRAWL_PRESENCE_ONLY|0
alhoshan|CRAWL_PRESENCE_ONLY|0
alkhaas|CRAWL_PRESENCE_ONLY|0
alobid|CRAWL_PRESENCE_ONLY|0
amlakalahsa|CRAWL_PRESENCE_ONLY|0
aouj|CRAWL_PRESENCE_ONLY|0
aqaratikom|CRAWL_PRESENCE_ONLY|0
aqarmonthly|CRAWL_PRESENCE_ONLY|0
arkaan|CRAWL_PRESENCE_ONLY|0
awal|CRAWL_PRESENCE_ONLY|0
azdad|CRAWL_PRESENCE_ONLY|0
bahadhabab|CRAWL_PRESENCE_ONLY|0
eaqartabuk|CRAWL_PRESENCE_ONLY|0
erapulse|CRAWL_PRESENCE_ONLY|0
fursaghyr|CRAWL_PRESENCE_ONLY|0
gathern|DIRECT_REVISIT|0
gathern|DIRECT_REVISIT|1
jurash|CRAWL_PRESENCE_ONLY|0
october|CRAWL_PRESENCE_ONLY|0
ramzalqasim|CRAWL_PRESENCE_ONLY|0
rawasidark|CRAWL_PRESENCE_ONLY|0
sadin|CRAWL_PRESENCE_ONLY|0
satel|CRAWL_PRESENCE_ONLY|0
therc|CRAWL_PRESENCE_ONLY|0
